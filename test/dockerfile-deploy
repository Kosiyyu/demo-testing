FROM alpine:latest

RUN apk --no-cache add git maven

RUN git clone https://github.com/Kosiyyu/demo-testing.git /app

WORKDIR /app

RUN ls && \
    pwd && \
    cd test && \
    mvn package

CMD ["java", "-jar", "demo-testing/test/target/*.jar"]
